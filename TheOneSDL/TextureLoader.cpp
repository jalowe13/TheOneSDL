#include "TextureLoader.h"


bool TextureLoader::loadTexture(std::string file_name)
{
	//SDL_Texture* new_texture;
	//new_texture = IMG_LoadTexture(app.getRenderer(), file_name.c_str());
	//if (file_name.rfind("VGB"))
	//{
	//	/*app.getPlayer().setTexture()*/
	//}
	return false;
}

/*
* 		player_texture = IMG_LoadTexture(renderer, "VGB_Idle.png");
		SDL_QueryTexture(player_texture, NULL, NULL, &textureWidth, &textureHeight);
		frameWidth = textureWidth / 60;
		frameHeight = textureHeight;
		player->xTexEdit(0);
		player->yTexEdit(0);
		player->wTexEdit(frameWidth);
		player->hTexEdit(frameHeight);
*/